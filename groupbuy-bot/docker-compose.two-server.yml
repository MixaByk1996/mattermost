# Docker Compose configuration for Two-Server Deployment
# Server 1: Chat service (WebSocket real-time messaging)
# Server 2: Bot service with business logic and data
#
# This configuration splits the application across two servers:
# - Server 1 (Chat): Handles real-time WebSocket chat, optimized for concurrent connections
# - Server 2 (API/Bot): Handles business logic, bot commands, and data storage
#
# Usage:
# On Server 1 (Chat): docker-compose -f docker-compose.two-server.yml up chat-server redis-chat nginx-chat
# On Server 2 (API):  docker-compose -f docker-compose.two-server.yml up core bot telegram-adapter postgres redis-api

version: '3.8'

services:
  # ============================================
  # SERVER 1: Chat Services
  # ============================================

  # WebSocket server for real-time chat
  websocket-server:
    build: ./infrastructure/websocket
    container_name: groupbuy-chat-server
    command: python chat_server.py
    restart: unless-stopped
    environment:
      - REDIS_URL=redis://redis-chat:6379/0
      - CORE_API_URL=${CORE_API_URL:-http://core:8000/api}
      - JWT_SECRET=${JWT_SECRET:-your-jwt-secret}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    ports:
      - "8765:8765"
    depends_on:
      redis-chat:
        condition: service_healthy
    networks:
      - chat-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8765/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Redis for chat message caching and pub/sub
  redis-chat:
    image: redis:7-alpine
    container_name: groupbuy-redis-chat
    command: redis-server --appendonly yes --maxmemory 256mb --maxmemory-policy allkeys-lru
    restart: unless-stopped
    volumes:
      - redis_chat_data:/data
    ports:
      - "6380:6379"
    networks:
      - chat-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Nginx for chat server (SSL termination, load balancing)
  nginx-chat:
    image: nginx:alpine
    container_name: groupbuy-nginx-chat
    restart: unless-stopped
    volumes:
      - ./infrastructure/nginx/nginx-chat.conf:/etc/nginx/nginx.conf:ro
      - ./infrastructure/nginx/ssl:/etc/nginx/ssl:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - websocket-server
    networks:
      - chat-network

  # ============================================
  # SERVER 2: API and Bot Services
  # ============================================

  # Core Django API (business logic and data)
  core:
    build: ./core
    container_name: groupbuy-core-api
    command: >
      sh -c "python manage.py migrate --noinput &&
             python manage.py collectstatic --noinput &&
             gunicorn config.wsgi:application --bind 0.0.0.0:8000 --workers 4"
    restart: unless-stopped
    volumes:
      - ./core:/app
      - ./frontend:/app/../frontend:ro
      - static_volume:/app/staticfiles
      - media_volume:/app/media
    environment:
      - DATABASE_URL=postgresql://${DB_USER:-postgres}:${DB_PASSWORD:-password}@postgres:5432/${DB_NAME:-groupbuy}
      - REDIS_URL=redis://redis-api:6379/0
      - SECRET_KEY=${SECRET_KEY:-your-secret-key-here}
      - DEBUG=${DEBUG:-False}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-*}
      - CORS_ALLOWED_ORIGINS=${CORS_ALLOWED_ORIGINS:-}
      - WEBSOCKET_URL=${WEBSOCKET_URL:-ws://chat-server:8765}
    depends_on:
      postgres:
        condition: service_healthy
      redis-api:
        condition: service_healthy
    ports:
      - "8000:8000"
    networks:
      - api-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Telegram Bot service
  bot:
    build: ./bot
    container_name: groupbuy-bot
    command: python main.py
    restart: unless-stopped
    volumes:
      - ./bot:/app
    environment:
      - CORE_API_URL=http://core:8000/api
      - REDIS_URL=redis://redis-api:6379/1
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
      - YOOKASSA_SHOP_ID=${YOOKASSA_SHOP_ID}
      - YOOKASSA_SECRET_KEY=${YOOKASSA_SECRET_KEY}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
    depends_on:
      - core
      - redis-api
    networks:
      - api-network

  # Telegram adapter
  telegram-adapter:
    build: ./adapters/telegram
    container_name: groupbuy-telegram-adapter
    command: python adapter.py
    restart: unless-stopped
    environment:
      - BOT_SERVICE_URL=http://bot:8001
      - TELEGRAM_TOKEN=${TELEGRAM_TOKEN}
    depends_on:
      - bot
    networks:
      - api-network

  # PostgreSQL database
  postgres:
    image: postgres:16-alpine
    container_name: groupbuy-postgres
    restart: unless-stopped
    environment:
      - POSTGRES_DB=${DB_NAME:-groupbuy}
      - POSTGRES_USER=${DB_USER:-postgres}
      - POSTGRES_PASSWORD=${DB_PASSWORD:-password}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    networks:
      - api-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis for API caching and session storage
  redis-api:
    image: redis:7-alpine
    container_name: groupbuy-redis-api
    command: redis-server --appendonly yes
    restart: unless-stopped
    volumes:
      - redis_api_data:/data
    ports:
      - "6379:6379"
    networks:
      - api-network
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Nginx for API server (reverse proxy, static files)
  nginx-api:
    image: nginx:alpine
    container_name: groupbuy-nginx-api
    restart: unless-stopped
    volumes:
      - ./infrastructure/nginx/nginx-api.conf:/etc/nginx/nginx.conf:ro
      - ./infrastructure/nginx/ssl:/etc/nginx/ssl:ro
      - static_volume:/app/staticfiles:ro
      - media_volume:/app/media:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - core
    networks:
      - api-network

volumes:
  postgres_data:
  redis_api_data:
  redis_chat_data:
  static_volume:
  media_volume:

networks:
  api-network:
    driver: bridge
  chat-network:
    driver: bridge
